/**
 * Shared pose domain types.
 * Re-exports from features/poses for convenience.
 */

export type { Pose, Category, SearchQuery, PagedResult, Difficulty, Orientation } from '@/features/poses/types';
