/**
 * Onboarding screen (src/app mirror) — full implementation lives in
 * app/(auth)/onboarding.tsx (the Expo Router entry point).
 *
 * This file re-exports from the canonical location so any imports against
 * the src/ tree resolve correctly.
 *
 * [Req 2]
 */
export { default } from '../../../app/(auth)/onboarding';
