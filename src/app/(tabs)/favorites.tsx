/**
 * src/app/(tabs)/favorites.tsx — source mirror (not served by Expo Router).
 * The active favorites screen implementation lives at: app/(tabs)/favorites.tsx (repo root).
 * [Req 18]
 */
import { View, Text } from 'react-native';

export default function FavoritesScreenMirror() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>See app/(tabs)/favorites.tsx for the full implementation.</Text>
    </View>
  );
}
