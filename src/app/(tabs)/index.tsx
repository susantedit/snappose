/**
 * src/app/(tabs)/index.tsx — source mirror (not served by Expo Router).
 * The active home screen implementation lives at: app/(tabs)/index.tsx (repo root).
 * [Req 4]
 */
import { View, Text } from 'react-native';

export default function HomeScreenMirror() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>See app/(tabs)/index.tsx for the full implementation.</Text>
    </View>
  );
}
